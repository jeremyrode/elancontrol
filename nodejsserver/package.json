{
  "name": "nodejsserver",
  "version": "0.0.0",
  "description": "Current work in here, getting a HTML/Javascript fronend connected to Node via Websocket to call the C function via N-API",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "node-gyp rebuild"
  },
  "author": "",
  "license": "ISC",
  "gypfile": true,
  "dependencies": {
    "serialport": "^9.2.0",
    "websocket": "^1.0.34"
  }
}
